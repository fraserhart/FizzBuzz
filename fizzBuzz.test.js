const fizzBuzz = require("./fizzBuzz.js");
/*
FizzBuzz

When a number is passed in to FizzBuzz:
    - Return that number unless it meets one of the following criteria:
    - When a number divisible by 3 is passed in, return `Fizz`
    - When a number divisible by 5 is passed in, return `Buzz`
    - When a number divisible by 3 AND 5 is passed in return `FizzBuzz`
*/

test("example test", () => {
  expect(1).toBe(1);
});
